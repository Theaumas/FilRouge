{{ form_start(form, {'method': 'POST', 'action': path(user is defined and user.id is not null ? 'app_user_edit' : 'app_user_new', user is defined and user.id is not null ? {'id': user.id} : {}) }) }}

    {{ form_widget(form) }}
    <button class="btn btn-primary" type="submit">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}